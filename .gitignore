# Org dataset — do not push
candidates.jsonl
*.jsonl

# Raw submission backup
submission.csv

# Python cache
__pycache__/
*.pyc
# Only ignore .streamlit at root level, not inside subfolders
/.streamlit/
.env